package Core.Class;

public class ClassAndInstansOf {
    /*
    в чем разница между использованием getClass() и instanceof operator в Java?
    Ключевое различие между ними заключается в том, что getClass() возвращает true только в том случае,
    если объект на самом деле является экземпляром указанного класса,
    но оператор instanceof может возвращать true,
    даже если объект является подклассом указанного класса или интерфейса в Java.
     */
}
